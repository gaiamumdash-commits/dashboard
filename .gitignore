# Dependências
node_modules/

# Next.js
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

# Vercel
.vercel/

# Logs
*.log
npm-debug.log*

# Editor / SO
.vscode-test/
.DS_Store

# Segredos e ambiente (nunca versionar)
.env
.env.local
.env.*.local
!.env.local.example

# Supabase CLI (estado local)
supabase/.temp/

# Export local de dados gerados em runtime (metas SMART em Markdown) — não é
# código-fonte, é saída dinâmica do app, cada ambiente gera o seu.
ecc-system/

# Environment & Secrets (AIOX)
*.key
*.pem

# Dependencies (AIOX)
node_modules

# Build & Logs (AIOX)
dist/
logs/

# IDE & OS (AIOX)
Thumbs.db
.idea/
*.swp

# AIOX Local (AIOX)
.aiox-core/local/
.claude/settings.local.json
.aiox/install-log.txt

# AIOX — framework vendorizado (51MB, reconstituível via `aiox install`), nunca versionar
.aiox-core/

# AIOX — config de IDEs que não usamos neste projeto (só Claude Code)
.antigravity/
.codex/
.cursor/
.gemini/
.kimi/
